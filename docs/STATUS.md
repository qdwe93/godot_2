# STATUS — 진행 상황 및 재개 가이드

> **이 파일만 읽으면 작업을 이어서 할 수 있어야 한다.** 컴퓨터가 꺼져도, 세션이 끊겨도 여기서부터 재개한다.

**최종 갱신**: 2026-08-16
**현재 마일스톤**: **M22 완료 · 실기 확인 완료 · v0.22.0 릴리즈 완료 (밝은 바닥과 배경 아트 — 회보라 포석 타일 + 구운 외곽선).**
> 직전: M21 (감각 폴리시 — 색종이 · 무적 깜빡임 · 숫자키 · 획득음)
> 직전: M20 (음량 설정 — 일시정지 메뉴)
> 직전: M19 (오디오 — 배경음악 + 효과음 5종)
> 직전: M18b (HUD 소품 + 실기 지적 3건), 실기 확인 완료 (2026-08-14)

> ⚠️ **밝기 순서 규칙은 M22에서 폐기됐다** ([D-020](DECISIONS.md)). `docs/ASSETS.md`
> 3절의 "규칙 1 — 밝기 위계"와 "외곽선 금지"는 근검정 바닥 시절의 기록이다.
> 지금 기준은 **타일 PNG의 어두운/밝은 톤 양쪽에서 외곽선 3:1 + 본체 색차 dE 25**다.
> 상세는 [devlog 023](devlog/023-m22-bright-floor.md).

> 🚫 **밸런스는 건드리지 않는다 (사용자 지시, 2026-08-14).** 난이도·수치·경험치 경제는
> 지금 상태로 동결이다. 사용자가 따로 지시하기 전까지 곡선도 자석 반경도 젬 수명도
> 손대지 말 것. **실기 스크린샷에 젬이 수백 개 쌓여 있는 것을 보고 문제로 보고했는데,
> 화면 확인을 위해 일부러 가만히 서 있던 플레이였다.** 스크린샷 한 장으로 밸런스를
> 추론하지 말 것 — 밸런스 판단은 `diag_balance` 실측으로만 한다.

> ⚠️ **자동화 실행에는 반드시 `--auto-play`.** 게임은 타이틀에서 일시정지한 채 시작하고 레벨업에서도 멈춘다.
> ⚠️ **측정은 `tests/diag_balance.tscn`으로 한다.** 매번 새로 쓰지 말 것 (M10에서 그러다 같은 함정에 반복해서 걸렸다).

---

## 한 줄 요약

**M22로 밝은 바닥(#8)을 가져왔다. 레퍼런스에서 남은 것은 코인/메타 성장(#10)
하나뿐이다.** 고정식 조이스틱은 사용자가 지금 방식 유지로 결정했다.

바닥은 근검정 위 #1E1E26 격자에서 **밝은 회보라 포석 타일**(512px, #A79BD2 ~
#C0B0E4)이 됐고, 잃어버린 대비는 **스프라이트에 구운 어두운 외곽선**이 되찾는다.
기존 그림 8종을 하나도 다시 만들지 않았다. 경위는
[devlog 023](devlog/023-m22-bright-floor.md).

**게임이 "탕탕특공대처럼" 보이고 조작되기 시작했다.** M16에서 카메라가 들어와
주인공이 화면 한가운데 고정되고 세계가 흐른다. 캐릭터는 3배가 됐고 주인공은
사람이 됐다.

카메라 때문에 판이 1.9배 길어졌던 것은 **곡선 재조정으로 되돌렸다**
(4.0/5.0 -> 8.0/9.0). 최선 빌드 생존 **792 / 805 / 1047초 (중앙값 13분 25초)**,
PRD 목표 10~15분 안이고 **3회 모두 사망으로 끝난다.**

| | M12b | M16 |
|---|---:|---:|
| 무입력 | 95초 | **119초** |
| 봇 · 최선 빌드 | 572~900초 (평균 808초) | **792~1047초 (중앙값 805초)** |
| 도달 레벨 | 24~42 | **45~58** |
| 상한에 걸린 판 | 0/3 | **0/3** |

근거와 시행착오는 [devlog 019](devlog/019-m16-camera-and-scale.md).
M17·M18 화면 작업은 [devlog 020](devlog/020-m17-m18-levelup-and-hud.md).

시간보다 중요한 변화는 이것이다 — **조작과 선택이 결과를 가른다.**
M12a에서는 무입력 68.7초 대 최선 빌드 100.5초로 차이가 1.5배뿐이었다. 지금은
무입력 95초 대 최선 빌드 900초로 **9.4배**다. 업그레이드도 25개 중 4~6개만 찍던 것이
39개 중 24~39개까지 간다.

| | M12a | M12b |
|---|---:|---:|
| 무입력 | 68.7초 | **95초** |
| 봇 · 무작위 선택 | 71~96초 | **516초** |
| 봇 · 최선 빌드 | 79~101초 | **572~900초** |
| 도달 레벨 | 5~7 | **24~42** |
| 최종 화력 | 27~32 dps | **207~710 dps** |

근거와 시행착오는 [devlog 018](devlog/018-m12b-power-and-curve.md).

> ⚠️ **밸런스 수치를 만지기 전에 devlog 018을 읽어라.** 특히 ① 곡선은
> `tools/balance_sim.py`로 생성하고 손으로 적지 않는다 ② `enemies_per_spawn`은 1로
> 고정한다 ③ **시뮬레이터로 절대 시간을 예측하지 않는다** (실측과 1.8배 어긋났다).


## 완료된 것

### M0 — 프로젝트 셋업
- [x] codex 표준 호출 방식 확정 → `CLAUDE.md`
- [x] `project.godot` (1280×720, gl_compatibility), 폴더 구조, `.gitignore`
- [x] 헤드리스 임포트 + 실행 검증 파이프라인
- [x] 문서 골격 (PRD / GDD / DECISIONS / devlog / CLAUDE.md)

### M1 — 플레이어 이동
- [x] InputMap: `move_left/right/up/down` ← WASD **물리 키코드**
- [x] `scenes/player.tscn` (CharacterBody2D, 반지름 12 원형 충돌, 하늘색 24×24 플레이스홀더)
- [x] `scripts/player.gd` — 8방향 이동 200px/s, 화면 경계 제한
- [x] `scripts/capture_helper.gd` — 게임 화면 PNG 저장 도구 (시각 검증용)
- [x] `tests/test_player_movement.tscn` — 프로젝트 최초 자동 테스트 (3케이스 통과)
- [x] 부팅 자가진단 `BOOT_OK actions_ok=true`

### M2 — 적 스폰 + 추적
- [x] `scenes/enemy.tscn` / `scripts/enemy.gd` — 직진 추적, 속도 60, 레이어 2 / 마스크 1
- [x] `scripts/enemy_spawner.gd` — 화면 밖 원주(반지름 794.3) 스폰, 간격 1.5초, 상한 30
- [x] `main.tscn`에 `EnemyContainer` 도입 — **모든 적은 이 아래에만 붙인다**
- [x] `tests/test_enemy_spawn.tscn` — 4케이스 통과 (주기 스폰 / 화면 밖 / 추적 / 상한)
- [x] 부팅 토큰 규약 변경: `BOOT_OK milestone=M2 ...`

### M3 — 자동 공격
- [x] `scenes/projectile.tscn` / `scripts/projectile.gd` — Area2D 투사체, 속도 400, 수명 3초
- [x] `scripts/weapon.gd` — 쿨다운 0.5초, 가장 가까운 적 조준, **사거리 350**
- [x] `enemy.gd` — `take_damage()`, HP 0에 `queue_free()`, `died` 시그널(M5에서 사용)
- [x] `tests/test_weapon.tscn` — 7케이스 통과
- [x] 캡처 도구를 **시간 기준**(`--capture-after=<초>`)으로 전면 교체

### M4 — 접촉 피해와 체력
- [x] `player.gd` — HP 100, 무적 0.5초, `take_damage()`, `health_changed`/`died` 시그널
- [x] `player.tscn`에 `Hurtbox`(Area2D) — **시그널이 아니라 매 프레임 겹침 폴링** (D-012)
- [x] `main.gd` — `PLAYER_HEALTH`/`PLAYER_DIED` 로그, 사망 시 스포너 정지 + 게임오버 라벨
- [x] `tests/test_player_damage.tscn` — 5케이스 통과
- [x] **테스트 하네스 무결성 강화** (D-013) — 거짓 통과 차단, 고장 주입으로 검증
- [x] M1 경계 테스트의 헤드리스 SKIP 해제 (구현이 ProjectSettings 기반으로 바뀌어 검증 가능해짐)

### M5a — 경험치 젬과 자석 수집
- [x] `xp_gem.tscn`/`xp_gem.gd` — 레이어 8(=4번), 자석에 끌려와 수집 시 경험치 지급
- [x] `pickup_spawner.gd` — 적 `died` 시그널로 드랍. **물리 스텝 회피 위해 `call_deferred`** (D-015)
- [x] 플레이어 `MagnetArea`(반경 60) — 시그널이 아니라 폴링 수집
- [x] `level_system.gd` — 곡선 `5+(레벨-1)*3`, 다중 레벨업 + 잔여 경험치 이월
- [x] `tests/test_experience.tscn` — 5케이스. **전체 5스위트 24케이스 통과, 런타임 에러 0**

### M5b — 레벨업 일시정지 UI
- [x] `level_up_ui.tscn`/`level_up_ui.gd` — CanvasLayer, **`process_mode = 3`(WHEN_PAUSED)**
- [x] 연속 레벨업 대기열 (젬 하나로 여러 레벨 → 한 번에 하나씩 선택)
- [x] `upgrade_chosen(id)` 시그널 — M6이 여기에 효과를 연결하면 UI는 수정 불필요
- [x] `tests/test_level_up_ui.tscn` — 5케이스. **전체 6스위트 29케이스 통과**
- [x] 실제 게임 캡처로 UI 표시 확인 (강제 레벨업 진단)

### M6a — 업그레이드 효과 적용
- [x] `upgrade_data.gd` — 정의 단일 출처 (id / 라벨 / 최대레벨 / 수치)
- [x] `upgrade_manager.gd` — **기본값 캡처 후 `base × f(level)` 재계산** (현재값 곱하기 금지)
- [x] 쿨다운 변경 시 **실행 중인 Timer.wait_time도 갱신** (안 하면 체감 안 됨)
- [x] 자석 반경은 `CircleShape2D.duplicate()` 후 변경 (공유 리소스 오염 방지)
- [x] **기존 스크립트 무수정**, 노드 추가만으로 구현
- [x] `tests/test_upgrades.tscn` — 5케이스. **전체 7스위트 34케이스 통과**

### M6b — 신규 무기와 레벨 상한
- [x] 왕관 경험치 배율을 실제 젬 경로에 연결 (`upgrade_manager` **그룹**으로 지연 조회, 매니저 없으면 원본 그대로)
- [x] `max_level` 상한 적용 — 남은 선택지 1~2개면 그만큼만, **0개면 화면 생략**(안 그러면 영구 정지)
- [x] 산탄 — 전방 부채꼴 3발 (-15°/0°/+15°), 초기 잠금
- [x] 궤도구 — **플레이어의 자식 노드로 공전**, 적별 피해 쿨다운, 초기 잠금
- [x] `tests/test_new_weapons.tscn` — 5케이스. **전체 9스위트 44케이스 통과**

### M7a — 난이도 곡선과 적 변종
- [x] `wave_data.gd` — 6페이즈 (간격 1.2→0.3초, 상한 40→160, 다중 스폰 1→3, **HP 배율 1.0→3.5**)
- [x] 적 3종(기본/빠른/탱커)을 **씬 복제 없이** 스탯 주입으로 구현
- [x] HP 배율은 **`add_child()` 이전에** 적용 (`_ready()`에서 `health = max_health`이므로)
- [x] `use_wave_data = false`로 기존 수동 설정 동작 보존
- [x] `tests/test_waves.tscn` — 6케이스. **전체 10스위트 50케이스 통과**
- [x] **실측: 정지 상태 플레이어가 74초에 사망** (이전에는 무한 생존). 적 63마리에서 145fps

### M7b — 보스와 적 분리
- [x] `boss_spawner.gd` — 300초에 **정확히 1마리**, 웨이브 가중치와 분리된 1회성 이벤트
- [x] `enemy.gd` 분리 로직 — 4프레임마다 재계산 + 인스턴스ID 분산 + 이웃 12개 상한 (O(n²) 회피)
- [x] `tests/test_boss_and_separation.tscn` — 6케이스. **전체 11스위트 62케이스 통과**
- [x] **실측: 최대 161마리에서 최저 144fps** → **오브젝트 풀링 도입 불필요 확정** (D-006 종결)
- [x] 테스트 실행에 `--quit-after 3600` 추가 (파싱 실패 시 무한 대기 방지)

### M8 — HUD와 화면 흐름
- [x] `hud.tscn`/`hud.gd` — HP·경험치·레벨·시간·처치 수, `process_mode = 3`(ALWAYS)
- [x] 시그널 연결과 별개로 `_ready()`에서 초기값을 채움 (안 하면 첫 변화 전까지 공백)
- [x] 킬 카운트는 `EnemyContainer.child_entered_tree`로 범위를 좁혀 연결
- [x] `game_flow.tscn`/`game_flow.gd` — 타이틀 → 플레이 → 게임오버 → 재시작
- [x] 재시작은 **`paused = false` 먼저, 그 다음 `reload_current_scene()`**
- [x] `--auto-play` — 타이틀 건너뛰기 + 레벨업 자동 선택 (자동화 필수)
- [x] `tests/test_hud` 6케이스 + `tests/test_game_flow` 6케이스. **전체 13스위트 74케이스 통과**

### M9 — 폴리시
- [x] `assets/vfx/` — Brackeys VFX 번들(CC0)에서 스프라이트시트 2종 + LICENSE.txt 추출. **PC 보유분이라 다운로드 없음**
- [x] `hit_effect.tscn`/`gd` — `Sprite2D`의 `hframes`/`vframes`로 플립북 재생 후 자가 해제
- [x] `effect_spawner.gd` — 사망 이펙트를 **적의 자식이 아니라 EffectContainer**에 생성
- [x] `screen_shake.gd` — 5px/0.15초, **종료 시 원위치 정확히 복원**. 배경을 40px 확장해 가장자리 노출 방지
- [x] 카메라는 도입하지 않음 (경계 계산 전면 변경 회피, D-007)
- [x] `tests/test_effects.tscn` — 6케이스. **전체 14스위트 80케이스 통과**
- [ ] 사운드 — 보유 팩에 오디오가 없어 미실시 (별도 다운로드 필요)

### M10 — 밸런싱 (1차)
- [x] 봇 진단 확립 — `Input.action_press()`로 실제 조작을 흉내 내 생존 시간 측정
- [x] **난이도 곡선이 240초에서 끊겨 있던 구조적 결함 발견 → 780초까지 4페이즈 확장**
- [x] 실측: 정지 74.0초 / 봇 조작 74.7초 → **조작이 이득을 주지 못한다**
- [x] 측정 규약 문서화 (일시정지 중 `physics_frame` 미발생, 출력 버퍼링, `--quit-after` 필수)
- [ ] **목표 10~15분 미달성.** 시작 시점 완화를 시도했으나 테스트가 수치를 하드코딩해 되돌림

### M11a — 6인 리뷰 반영 (배선 감사 · 시각 피드백)
- [x] 페르소나 3종 정의 + A팀(Claude)/B팀(codex) **6개 독립 리뷰** → `docs/review/`
- [x] **산탄·궤도구가 게임에서 획득 불가능했던 문제 수정** — `UPGRADE_POOL` 제거, `UpgradeData.get_all_ids()`로 단일 출처화
- [x] **도달 불가능한 난이도 페이즈 수정** — `wave_data.gd` 페이즈 5 `start_time` 120.0 → 240.0
- [x] **보스가 젬을 안 떨어뜨리던 문제 수정** — `spawn_pickup` → `on_enemy_died`, `PickupSpawner` 그룹 등록
- [x] 명중 이펙트 연결(`spawn_hit()` 호출처가 0건이었다), 적 피격 0.06초 흰색 번쩍
- [x] 위험 상태 표시 — HP 30% 이하에서 HP바 적색 + 화면 비네트. HP바(녹색)/경험치바(청색) 색 분리
- [x] 보스 색 대비 확보 — 배경 대비 **1.05:1 → 약 6.5:1**
- [x] `test_waves.gd` **불변식 기반 전환** (튜닝 봉쇄 해제)
- [x] `tests/test_scene_wiring` 신규 6케이스 — 그룹·메서드·시그널·선택지 노출을 실제 `main.tscn`으로 검증
- [x] `tests/test_feedback` 신규 5케이스
- [x] **고장 주입 6건 전부 FAIL 확인** (새 테스트가 원래 버그를 잡는다)
- [x] **전체 16스위트 87케이스 통과**
- [x] Windows / 웹 / Android 빌드 → `docs/BUILD.md`, `export_presets.cfg`
- [x] 에셋 조사 — **codex 내장 `imagegen` 사용 가능** (`codex features list` → `image_generation stable true`). 최초에 "기능 없음"으로 오판했다가 정정. 사양·프롬프트·생성 운영 규칙을 `docs/ASSETS.md`에 기록. **에셋 생성 자체는 아직 미실행**

### M18b — 실기 지적 3건 (그리기 순서·화면 가림·흐르는 시계)
- [x] **몹이 젬 뒤로 지나가던 문제** — 월드 컨테이너 `z_index` 를 명시. 바닥(-100) < 젬(-10) < 적(0) < 주인공(10) < 투사체(20) < 이펙트(30)
- [x] **몹이 경험치 바 뒤로 지나가던 문제** — 바를 y=122 에서 **y=6 (화면 맨 위)** 로. HUD 는 CanvasLayer 라 항상 세계 위에 그려지므로 겹침을 줄이려면 가장자리로 미는 수밖에 없다
- [x] **시계가 "스킬 선택" 을 가리던 문제** — `CanvasLayer.layer` 기본값이 0 이 아니라 **1** 이라 HUD 와 레벨업이 동률이었다. LevelUpUI 를 layer 5 로. 덮개도 완전 불투명으로 (0.985 로도 유령이 읽혔다)
- [x] **일시정지 중에도 HUD 시계만 흐르던 문제** — 백로그 A-engineer #8. `get_tree().paused` 면 시계만 멈춘다
- [x] 케이스 3개 추가 + **고장 주입 4건 전부 FAIL 확인.** 셋 다 기존 141케이스가 전부 통과하는 상태에서 나온 문제다
- [x] **전체 23스위트 144케이스 통과**
- [x] **실기 확인 (2026-08-14)** — 사용자 평: "1,2,3번 정확히 고쳐졌어"

### M18 — HUD 소품 4종 (레퍼런스 반영)
- [x] **일시정지 버튼** (88x88, 좌상단) + 일시정지 메뉴. 지금까지 일시정지 수단이 레벨업 때 말고는 없었다
- [x] 일시정지 상태는 `GameFlow` 가 계속 **단독 소유**한다. 타이틀·게임오버·레벨업 중에는 거부 (`can_pause()`)
- [x] **타이머를 상단 한가운데로**, 폰트 24 -> 48, 검은 외곽선 10
- [x] **경험치 바를 화면 가로 전체로**. `Lv.N` 을 왼쪽 탭으로
- [x] **체력바를 주인공 발밑으로.** HUD 좌상단 체력바·숫자는 제거 (중복)
- [x] **피해 숫자** — 명중 지점에 뜨고 위로 떠오르며 사라진다. 상한 40개 (적 219마리 + 산탄 7발이면 초당 수백 개가 된다)
- [x] 배선 케이스 — `spawn_damage_number` 가 **있는 것**과 투사체가 **부르는 것**은 다르다. 실제 명중으로 검증
- [x] 캡처로 잡은 것 3가지: 발밑 체력바가 처음부터 비어 있음(자식 `_ready()` 가 부모보다 먼저 돈다), 메뉴 버튼 반투명, 빈 경험치 바가 꽉 차 보임
- [x] `tests/test_hud_props` 신규 14케이스 + **고장 주입 11건 전부 FAIL 확인**
- [x] **전체 23스위트 141케이스 통과**
- [x] 성능: 적 **219마리에서 145fps** (M16 과 동일). 밸런스 실측 880.8초로 목표 범위 유지

### M17 — 레벨업 화면 개편 (레퍼런스 반영)
- [x] **카드 3장** — 이름 / 아이콘 / 설명 / 별 등급. 레퍼런스([REFERENCE_UI.md](REFERENCE_UI.md)) 구조
- [x] **별 등급** — 개수는 그 스킬의 `max_level`(3~8), 채워진 별은 **찍고 난 뒤의 레벨**. 백로그의 "레벨업 창이 현재 레벨을 안 보여준다" 해결
- [x] `New!` 리본 — 아직 한 번도 안 찍은 것
- [x] **보유 슬롯 바** — 무기 2칸 / 강화 6칸. 획득한 것만 아이콘 + 레벨 숫자. 업그레이드 8종을 볼 방법이 화면에 전혀 없었다
- [x] 이름·설명은 `label`("이름 - 설명")에서 갈라 쓴다. 두 벌로 저장하면 갈라진다
- [x] 슬롯·별은 씬이 아니라 **코드에서 `UpgradeData` 정의 순서대로** 만든다
- [x] 아이콘 8종 (codex imagegen). 여백을 알파 경계로 잘라 크기 통일 → [ASSETS.md 9절](ASSETS.md)
- [x] 카드 불투명 처리 — 버튼 기본 스타일이 반투명이라 뒤의 주인공이 비쳤다 (캡처로 발견)
- [x] `tests/test_level_up_cards` 신규 8케이스 + **고장 주입 8건 전부 FAIL 확인**
- [x] Godot enum 5개를 기억으로 쓰지 않고 전부 찍어서 확인 (CLAUDE.md에 절차 기록)
- [x] **전체 22스위트 127케이스 통과**

### M16 — 실기 2차 피드백 (카메라·확대·사람형 주인공) — **실기 확인 완료**
- [x] **카메라 도입** — `Camera2D` 를 주인공의 자식으로. 주인공은 항상 화면 한가운데, 세계가 흐른다
- [x] 딸려온 것 5가지: 가두기 제거 / 스폰 기준을 `Arena.get_view_center()` 로 / 흔들림 대상을 카메라로 / 뒤처진 적 회수 / **흐르는 바닥 격자** (단색 배경이면 움직임이 안 보인다)
- [x] **캐릭터 3배** — 주인공 24→72px, 적 20→60px (변종 51/84/180). 충돌 반경·분리 반경·측정 도구 SPEC 도 같이
- [x] **주인공을 사람으로** — 정면을 보고 선 캐릭터. 24px에서는 불가능했고 72px이 되며 가능해졌다
- [x] **레벨업 화면 맞춤** — M14 앵커 전환에서 이것 하나만 빠져 있었다. 버튼 48→118 단위 (S21 약 16mm)
- [x] 젬·투사체·궤도구는 **보이는 크기만** 2배 (충돌 반경 불변 = 밸런스 불변)
- [x] `tests/test_camera_follow` 신규 7케이스 + `test_screen_fit` 에 레벨업 회귀 1케이스
- [x] **고장 주입 9건 전부 FAIL 확인.** 그중 레벨업 Dimmer 회귀는 **기존 21스위트가 통과시키고 있었다**
- [x] 전체 21스위트 119케이스 통과
- [x] 성능: 적 **232마리에서 145fps**. "풀링 불필요" 결론이 240마리 기준으로도 유지됨 확인
- [x] **실기 확인 (2026-08-14)** — 주인공 중앙 고정, 버튼 크기·반응, 난이도 전부 통과
- [x] 목표 화면 레퍼런스 등록 → [REFERENCE_UI.md](REFERENCE_UI.md). **화면 방향은 가로 유지**
- [x] **밸런스 재조정** — 곡선 4.0/5.0 -> 8.0/9.0. 근거는 시뮬이 아니라 **과거 실측끼리의 비율**(카메라가 판을 1.9배 늘렸다)이고, 예상 745초 대 실측 805초로 8% 안에 맞았다
- [x] 곡선 사본을 없앴다 — `--emit-gdscript` 로 `wave_data.gd` 를 찍고, `balance_model.py` 는 `balance_sim` 에서 가져온다

### M15 — 캐릭터 아트
- [x] 평면 도형 → **캐릭터 스프라이트 5종** (플레이어 원형 메카, 뿔 고블린, 삼각 돌격체, 갑충, 팔각 군주)
- [x] 젬·투사체·궤도구는 도형 유지 — 10·8·14px라 캐릭터가 성립하지 않는다
- [x] **밝기 위계 유지** — 8종 전부 `check_sprite_luminance.py` 통과, 플레이어 대비 12.80:1로 여전히 최상위
- [x] 실루엣 문법이 "기하 도형"에서 **"생물 실루엣"**으로 바뀜. 20px에서는 이쪽이 더 잘 구분된다
- [x] `Polygon2D` → `Sprite2D` 전환. **색 바꾸기 연출이 전부 깨지므로**(modulate는 곱셈) 흰색/빨간색 오버레이의 알파로 교체
- [x] codex 자체 판정이 또 틀렸다 — 이번엔 5장 전부 "실패"라 했는데 측정하니 3장 통과. **판정은 항상 측정 도구로**
- [x] 전체 20스위트 111케이스 통과

### M14 — 실기 피드백 (화면 맞춤·피격 연출)
- [x] **화면 잘림 해결** — `scripts/arena.gd` 단일 출처, 씬은 앵커 기반. 기기별 빌드 불필요
- [x] 배경을 `BackgroundLayer`(CanvasLayer, layer=-1)로. Control 앵커는 Node2D 부모 밑에서 동작하지 않는다
- [x] **어지러움 해결** — 원인은 피격이 아니라 **회복**이었다 (M12b 재생 도입 시 health_changed를 구분 안 함)
- [x] 흔들림 최소 간격 0.6초, 위험 표시를 화면 비네트에서 주인공 빨간 깜빡임으로
- [x] `tests/test_screen_fit` 신규 6케이스
- [x] 실기 측정: 사람이 **17분 42초 / 레벨 51 / 1792 처치** (봇 최선 빌드 13분 28초보다 길다)

### M13 — 모바일 터치 조작
- [x] `scenes/touch_joystick.tscn` — 화면 아무 데나 짚으면 생기는 **떠다니는 가상 조이스틱**
- [x] **`player.gd`를 한 줄도 안 고쳤다.** 조이스틱이 `Input.action_press()`로 InputMap의 `move_*`를 직접 눌러 키보드와 같은 경로로 흐르게 했다 — 덕분에 기존 이동 테스트와 봇 진단이 그대로 유효
- [x] `_unhandled_input` 사용 — GUI(타이틀 시작, 레벨업 3택)가 입력을 먼저 가져간다. `_input`을 쓰면 버튼이 안 눌린다
- [x] `process_mode = 3`(ALWAYS) — 일시정지 중에 손을 떼도 그 사실이 전달되어야 한다. 아니면 재개 시 그 방향으로 계속 달린다
- [x] `emulate_touch_from_mouse=true` — **PC·웹에서도 마우스 드래그로 시험 가능** (폰 없이 검증됨)
- [x] 창 모드 캡처로 실제 그려지는 것과 플레이어가 움직이는 것 확인
- [x] `tests/test_touch_joystick` 신규 6케이스 + **고장 주입 4건 전부 FAIL 확인**
- [x] **전체 19스위트 105케이스 통과**, 3플랫폼 재빌드

### M12b — 파워 곡선과 난이도 곡선 재설계
- [x] **60초 스폰 절벽 제거** — `enemies_per_spawn`을 1로 고정하고 `spawn_interval`만 줄이는 14페이즈 곡선. 페이즈 간 최대 점프 2.70배 → **1.30배**
- [x] 곡선을 손으로 적지 않는다 — `tools/balance_sim.py`의 `build_phases()`가 생성
- [x] **경험치 수입 상향** — 적 종류별 젬 값(탱커 3배, 보스 30), 자석 기본 반경 60 → 100
- [x] **화력 성장 신설** — 칼날(공격력 +30% ×8), 산탄 5레벨(3→7발), 궤도구 5레벨(피해 ×1→×5). 화력 상한 3.68배 → **71배**
- [x] **체력 재생 신설** (측정 중 발견) — 그전까지 체력은 오직 줄기만 해서 생존 시간이 "총 체력 / 잠식량"으로 못박혀 있었다. 기본 0.6/초 + 심장 레벨당 0.4/초
- [x] `tools/balance_sim.py` 신규 — 한 판을 초 단위로 시뮬레이션. **절대 시간 예측용이 아니다** (실측과 1.8배 어긋났다). 후보의 순서를 보는 데만 쓴다
- [x] `tests/test_power_growth` 신규 8케이스 + **고장 주입 6건 전부 FAIL 확인**
- [x] `test_player_damage`/`test_hud` 픽스처에서 재생을 꺼 격리 (허용 오차를 넓히지 않았다)
- [x] **전체 18스위트 99케이스 통과**


### M12a — 밸런스 재측정과 진단 교정
- [x] `tests/diag_balance.gd` 신규 — 실제 `main.tscn`을 띄워 봇이 조작하며 측정하는 **상설 도구**
  - `--pick=greedy` — 최선 빌드를 고정해 "선택이 나쁜 것"과 "게임이 약한 것"을 분리
  - `--speed=N` — `Engine.time_scale` 배속. `max_physics_steps_per_frame`도 같이 올려야 한다
- [x] `tools/balance_model.py` 신규 — 게임을 안 돌리고 처치율 대 스폰율·성장 비용을 계산
- [x] **9회 실측** — 무입력 68.7초 / 봇 무작위 71.4~96.2초 / 봇 최선 빌드 79.3~100.5초
- [x] 배속 검증 (1x 2회 vs 3x 4회) — 조건 간 차이 6.8초 < 조건 내 편차 21.2초
- [x] **원인 진단 교정** — 화력 상한은 1.52배가 아니라 3.68배이고, 애초에 구속 조건이 아니다
- [x] 실제 구속 조건 규명 — 60초 스폰 절벽(2.7배 점프)과 경험치 수입 부족(레벨 20까지 15.6분)
- [ ] **수치는 하나도 안 고쳤다.** M12b의 일이다

### M11b — 시각 위계 확정과 에셋 생성
- [x] **밝기 위계를 계산으로 확정** — 배경 대비 플레이어 12.92:1 > 보스 7.41 > 탱커 4.67 > 기본 4.33 > 빠른 3.85 > 젬 3.66 > 투사체 3.48, 전 요소 3:1 이상
- [x] **형태 문법 도입** — `ColorRect` → `Polygon2D`+`Line2D`. 원형(플레이어)/사각형(기본)/삼각형(빠른)/육각형+외곽선(탱커)/팔각형+외곽선(보스). 색각 이상 대응
- [x] 보스가 탱커와 실루엣이 겹쳐 육각형 → 팔각형으로 분리 (캡처로 발견)
- [x] `tests/test_visual_hierarchy` 신규 4케이스 — 색 하드코딩 없이 실제 씬·데이터에서 읽어 **관계**만 검사. 고장 주입 3건 FAIL 확인
- [x] **전체 17스위트 91케이스 통과**
- [x] 스프라이트 8종 생성 — codex `imagegen` 6종 + 코드 생성 2종. 전부 규칙 통과
- [x] 검사 도구 2종: `tools/check_sprite_luminance.py`, `tools/make_flat_sprites.py`
- [x] 생성 전 과정 기록 → `docs/ASSETS.md` 5절, `docs/asset_gen/` (작업자별 원문)
- [ ] **에셋은 씬에 미적용.** 도형과 결과가 같아 얻는 게 없고 `Sprite2D` 전환 비용만 생긴다 (ASSETS.md 5-8절)

### M19 — 오디오
- [x] `assets/audio/bgm/main_theme.mp3` — 사용자 제공 (4:03). 반복을 `.import` 와 코드 양쪽에서 켠다
- [x] `tools/gen_sfx.py` — 효과음 5종을 파이썬으로 생성 (발사/명중/피격/레벨업/사망). **라이선스 부담 없음**
- [x] `scripts/audio_manager.gd` — autoload `Audio`, ALWAYS, 이름별 솎아내기, 재생기 12개 고정 풀
- [x] `default_bus_layout.tres` — Master / Music(-8dB) / SFX
- [x] 호출 지점 6곳 (weapon, shotgun, effect_spawner, player, level_up_ui, game_flow)
- [x] `tests/test_audio` 신규 13케이스 + **고장 주입 10건 전부 FAIL 확인**
- [x] 실기 창 모드 40초 — 오디오 에러 없음, 60fps 유지
- [x] **M20에서 음량 설정 UI 와 저장 완료**

### M21 — 감각 폴리시 (색종이 · 무적 깜빡임 · 숫자키 · 획득음)
- [x] **레벨업 색종이 파티클** (REFERENCE_UI #7) — `CPUParticles2D`, 덮개 위·카드 뒤
- [x] **텍스처 없이 넣었더니 먼지가 됐다.** 1x1 흰 사각형이라 각속도를 줘도 도는 게 안 보인다. 12x20 흰 직사각형을 `make_ui_icons.py` 에 추가 (**캡처로만 발견한 결함 네 번째**)
- [x] **무적 시간 깜빡임** (A-designer #7) — `self_modulate` 알파 계단식. 위험 상태의 sin 맥동과 문법을 일부러 다르게 했다
- [x] **레벨업 숫자키 1/2/3 + 첫 카드 `grab_focus`** (A-designer #5) + 카드 좌상단 숫자 배지
- [x] **젬 획득음** — `pickup.wav` 50ms/peak 0.17 (여섯 중 가장 작다). 솎아내기는 `AudioManager` 가
- [x] **테스트가 실패를 SKIP 으로 숨기고 있던 것을 발견** — 배선이 끊겨도 헤드리스면 SKIP 으로 내려앉았다. 도피로 제거
- [x] **고장 주입 18건 전부 CAUGHT.** 1차 16건 중 3건을 놓쳤는데 원인이 셋 다 달랐다 (devlog 022 4절)
- [x] `tests/diag_levelup_capture.tscn` 신규 — 레벨업 화면 상설 캡처 도구
- [x] **전체 24스위트 177케이스 통과** (163 -> +14). 창 모드 45초 에러 0, 60fps
- [x] **밸런스 수치는 하나도 안 건드렸다** (동결 유효)
- [x] **codex 가 파일을 읽을 수 있게 됐다** — 아래 이슈 12 정정

### M20 — 음량 설정
- [x] `scenes/volume_settings.tscn` — 일시정지 화면 안, 배경음악/효과음 따로 10% 단위
- [x] **슬라이더가 아니라 −/+ 버튼 76×76.** 기본 HSlider 손잡이(16px)는 폰에서 못 잡는다
- [x] 버스 레이아웃의 믹스를 **100% 기준**으로 삼는다 (`base_db`)
- [x] `user://settings.cfg` 저장/복원. 없거나 깨져 있으면 기본값
- [x] 일시정지 덮개와 음량 바를 **불투명**으로 (캡처로 발견, 같은 함정 세 번째)
- [x] `test_audio` 13 → 19케이스 + **고장 주입 10건 전부 FAIL 확인**
- [x] ffmpeg 9.0 설치 — 이제 받아 온 음원을 자르고 변환할 수 있다

### M22 — 밝은 바닥과 배경 아트
- [x] **계획서의 한 줄이 성립하지 않는 것을 그림 만들기 전에 잡았다** — "대비 순서로
      바꾸되 순서는 유지"는 밝은 바닥에서 동시에 성립하지 않는다 (성립 상한 #6E6989)
- [x] 사용자 결정: 레퍼런스급 #A9A3C4 + **순서 규칙 폐기** ([D-020](DECISIONS.md))
- [x] `tools/make_ground_tile.py` 재작성 — 512px 불투명 무봉제 회보라 포석 타일,
      **명도 대역(0.28~0.58)을 저장 전에 스스로 검사한다**
- [x] `tools/sprite_outline.py` 신규 — 알파 팽창으로 어두운 외곽선을 굽는다.
      캔버스 크기를 유지하려 **잘라서 줄이고 가운데 놓은 뒤** 팽창시킨다.
      원본 `assets/sprites_src/` 보관, `--revert` 로 되돌아간다
- [x] `tools/check_sprite_luminance.py` 판정 재작성 — 배경을 **타일 PNG에서 두 톤**으로
      읽고, 외곽 대비 3:1 + 본체 색차 dE 25 를 **양쪽 톤에서** 본다.
      타일이 없으면 고정색 대체 없이 exit 2
- [x] **바닥색 단일 출처화** — `GRID_TINT` 제거, `read_tile_mean_colour()` 가 언더레이를
      PNG에서 유도한다 (예전엔 세 곳에 사본)
- [x] 젬·투사체·궤도구(`ColorRect`)는 씬에서 `Sprite` **뒤에** 2px `Outline` 추가
- [x] `test_visual_hierarchy` 재작성 5 → 6케이스 (밝기 순서·플레이어 최상위 삭제)
- [x] **캡처가 두 개를 더 잡았다** — 피해 숫자 `FADE_START` 0.35→0.5, 조이스틱을 흰색에서
      근검정으로. 둘 다 유닛 테스트는 초록불이었다
- [x] **전체 24스위트 178케이스 통과**, **고장 주입 8건 전부 잡음**
- [x] **그림 8종을 하나도 다시 만들지 않았다**
- [x] **밸런스 수치는 하나도 안 건드렸다** (동결 유효)

## 바로 다음에 할 일

밸런스는 다시 목표 안에 들어왔다. 남은 것은 **판의 질**이다.

1. ~~폰에서 새 빌드 확인~~ — **2026-08-14 확인 완료.** 사용자 평:
   "주인공 가운데, 버튼크기와 반응, 난이도 모두 아주 좋아." M16의 다섯 항목과
   재조정한 난이도가 실기에서 전부 통과했다
2. ~~자석 반경과 젬 수명~~ — **보류.** 밸런스 동결 중이다 (위 🚫 참고).
   봇 실측에서 안 주운 젬이 최대 254개까지 쌓이는 것은 사실이지만, 사용자가
   손대지 말라고 지시했다. 지시가 바뀌기 전까지 후보에서 뺀다
3. ~~3택 운의 편차를 줄인다~~ — **보류.** 밸런스 동결 중이다
4. ~~레벨업 창이 현재 레벨을 안 보여준다~~ — **M17에서 별 등급으로 해결**
5. ~~사운드 4종~~ — **M19 완료**, 음량 설정 **M20 완료**, 젬 획득음 **M21 완료.**
   남은 소리 후보는 **보스 전용 배경음악** 하나다
6. ~~레퍼런스 HUD 소품~~ — **M18 완료**, 색종이 파티클 **M21 완료**, 고정식
   조이스틱은 **사용자가 떠다니는 방식 유지로 결정** (2026-08-15),
   밝은 바닥 + 배경 아트 **M22 완료**.
   남은 것은 **코인/메타 성장** 하나뿐이다. 목록은 [REFERENCE_UI.md](REFERENCE_UI.md) 3절
7. ~~처치율이 스폰율의 82~85%에 고정되는 원인~~ — **보류.** 밸런스 동결 중이다

### ✅ M22 실기 확인 완료 (2026-08-16)

사용자가 Windows 빌드를 직접 실행했다. **"아트가 아쉽기는 해도 일단 문제는
없었던 거 같아."** 밝은 바닥·구운 외곽선·가독성에 결함 보고는 없었다.

**v0.22.0 릴리즈 발행 완료** — 태그 `v0.22.0-bright-floor`, Windows exe +
Android apk 첨부. M21이 릴리즈된 적이 없어 M21+M22를 한 릴리즈로 묶었다
(그래서 v0.21.0은 건너뛰었다).

---

## 다음 마일스톤 — M23 후보 (⚠️ **사용자 결정 대기 중**)

**2026-08-16 시점에 사용자가 아직 방향을 고르지 않았다.** 아래 4방향을 제시했고
답을 받으면 그대로 착수한다. 임의로 고르지 말 것.

사용자의 유일한 단서는 **"아트가 아쉽다"** 한마디다. 그래서 A를 추천했다.

### A. 사각형 3종을 그림으로 (추천, S~M)

**화면에서 가장 덜 그려진 세 요소가 젬·투사체·궤도구인데, 쓸 그림이 이미 디스크에
있으면서 한 번도 안 쓰였다.** 씬이 `ColorRect` 사각형을 그린다.

| 파일 | 실제 모양 | 지금 화면 |
|---|---|---|
| `assets/sprites/xp_gem.png` | 초록 **마름모** | 초록 정사각형 20px |
| `assets/sprites/projectile.png` | 주황 **짧은 캡슐** | 주황 정사각형 16px |
| `assets/sprites/orbital.png` | 청록 **원형 코어** | 청록 정사각형 28px |

`ASSETS.md` 3-0절의 형태 규칙과도, 레퍼런스("젬이 초록 다이아몬드")와도 맞는다.

작업 내용:
1. 세 씬을 `ColorRect` → `Sprite2D` 로 바꾸고 기존 PNG 연결
2. `tools/sprite_outline.py` 의 `DISPLAY_SIZES` 에 세 개 추가 (젬 20 / 투사체 16 /
   궤도구 28 — **`.tscn` 의 offset 은 반지름이라 두 배가 표시 크기다**)
3. `check_sprite_luminance.py` 의 `SPEC` 에 세 개를 **되돌려 넣는다.** 지금은
   "안 그려지니까 뺀다" 상태인데, 그리기 시작하면 **"그려지는 것만 검사한다"로
   다시 합쳐진다**
4. M22에서 임시로 깐 `Outline` 래퍼 ColorRect 제거 (구운 외곽선이 대체한다)
5. `tests/test_visual_hierarchy.gd` 의 `colorrect_elements_are_outlined` 와
   `outlined_elements_clear_minimum_contrast` 를 같이 고친다

⚠️ **함정**: `Sprite2D` 에는 `color` 가 없어 주입이 **에러 없이 조용히 죽는다.**
세 스크립트(`xp_gem.gd`/`projectile.gd`/`orbital.gd`)는 확인해 뒀고 `Sprite.color`
를 안 건드린다. 하지만 `test_visual_hierarchy.gd` 가 `Sprite.color` 를 **읽으므로**
같이 안 고치면 조용히 셋업 실패한다.

밸런스와 무관하다.

### B. 캐릭터 아트 재생성 (M~L)

**M22가 아트 제약을 크게 풀었다. 지금이 다시 뽑기에 가장 싼 시점이다.**

| | M21까지 | 지금 |
|---|---|---|
| 칠 색 | **어두운 색 금지** (근검정에 묻힌다) | 자유 — 외곽선이 대비를 낸다 |
| 외곽선 | **금지** (평균 밝기를 끌어내린다) | 도구가 구워 준다 |
| 통과 조건 | 휘도 순서 7종 + 3:1 | 두 톤에 대해 dE 25 |

예전엔 생성물 8장 중 6장이 검사에서 떨어졌다. 그 좁은 창이 사라졌다.
원본은 `assets/sprites_src/` 에 있고 `sprite_outline.py --revert` 로 언제든 되돌린다.

⚠️ **codex 의 생성 품질 자체 판정은 네 번 틀렸다** (M11b·M15·아이콘·사람형).
판정은 전부 `check_sprite_luminance.py` 측정과 캡처로 한다.

### C. 배경 아트 심화 (M)

M22에서 남겨 둔 도로·횡단보도 같은 모티프. **타일을 512보다 키워야 한다** —
512에 넣으면 반복이 격자처럼 읽힌다. 명도 대역(0.28~0.58)은 그대로 지켜야 하므로
화려함은 채도와 무늬로만 낸다.

### D. 아트 말고 기능

아래 "다음 후보" 표에서 고른다.

---

**다음 후보** (밸런스 동결이 유효한 동안 할 수 있는 것):

| 후보 | 크기 | 비고 |
|---|---|---|
| **코인 / 메타 성장 (REFERENCE_UI #10)** | **L** | 레퍼런스에서 **마지막 남은 항목**이다. 판 사이에 남는 진행이 생기므로 저장 구조가 필요하고, **경제를 건드리므로 밸런스 동결과 부딪힐 수 있다** — 착수 전에 사용자 확인이 필요하다 |
| **사각형 3종을 그림으로 ← A안, 추천** | S~M | M22 잔여. 위 "M23 후보 A" 참고 |
| 보스 전용 배경음악 | S~M | M19 잔여. 곡을 받아 와야 한다 (ffmpeg 은 이제 있다) |
| 화면 흔들림 최소 간격 재검토 | S | A-designer #9. M14에서 0.6초를 넣었으나 재측정은 안 했다 |
| 밸런스 데이터를 외부 파일로 분리 | M | A-engineer #7. 튜닝 루프 5단계 → 2단계. **동결이 풀릴 때를 대비하는 작업** |
| 반복 보스 / 페이즈 전환 알림 / 종료 조건 | M | A-planner. 종료 조건은 PRD 범위 판단이 필요하다 |

## M22 결과 — 밝은 바닥과 배경 아트 (완료, 2026-08-15)

계획대로 8단계를 밟았고 **1번(판정 기준)에서 계획서의 한 줄이 성립하지 않는 것을
먼저 잡아냈다.** 자세한 경위는 [devlog 023](devlog/023-m22-bright-floor.md).

### 무엇이 바뀌었나

| | M21까지 | M22 |
|---|---|---|
| 바닥 | 근검정 #14141A + #1E1E26 격자 (256px) | **회보라 포석 타일 512px** (#A79BD2 ~ #C0B0E4) |
| 대비 | 칠 색이 낸다 | **구운 어두운 외곽선**이 낸다 (#14141A) |
| 판정 배경 | 상수 하나 | **타일 PNG의 2/98 백분위 두 톤** |
| 순서 규칙 | 밝기 위계 7종 | **폐기** → 적 변종끼리 색차 dE 20 |
| 바닥색 출처 | 세 곳에 사본 | **타일 PNG 한 곳** |

**그림 8종은 하나도 다시 만들지 않았다.** 원본은 `assets/sprites_src/`,
산출물이 `assets/sprites/` 다 (`tools/sprite_outline.py --revert` 로 되돌아간다).

### 실측 (5종 전부 통과)

어두운 톤 #A79BD2 (0.3630) / 밝은 톤 #C0B0E4 (0.4786) 기준.

| 에셋 | 충전율 | 어두운톤 대비 | 밝은톤 대비 | 어두운톤 dE | 밝은톤 dE |
|---|---:|---:|---:|---:|---:|
| enemy_basic | 82.2% | 6.03:1 | 7.47:1 | 62.4 | 63.8 |
| enemy_boss | 65.0% | 6.32:1 | 7.91:1 | 42.5 | 44.9 |
| enemy_fast | 53.3% | 5.88:1 | 7.26:1 | 74.3 | 74.8 |
| enemy_tank | 77.6% | 6.11:1 | 7.59:1 | 37.0 | 43.5 |
| player | 69.1% | 6.22:1 | 7.76:1 | 29.8 | 30.8 |

전 스위트 **24개 178케이스 통과**, 고장 주입 **8건 전부 잡음**.

### 캡처가 잡은 것 두 개 (유닛 테스트는 초록불이었다)

- **피해 숫자**가 거의 안 읽혔다. 흰 글자는 밝은 바닥에서 2.4:1 뿐이라 검은
  외곽선이 혼자 떠받치는데 `modulate.a` 가 둘을 **함께** 흐린다. `FADE_START`
  0.35 → 0.5
- **터치 조이스틱**의 반투명 흰색이 바닥과 섞여 사라진다. 근검정으로 뒤집음

**밝은 바닥에서 대비를 내는 방향은 위가 아니라 아래다.** 근검정 시절의 습관
(반투명 흰색, 흰 글자)을 찾아 뒤집어야 한다.

### 아직 남은 것 (M22 범위 밖으로 남겨 둠)

- **젬이 아직 초록 정사각형이다.** 레퍼런스는 다이아몬드다. `xp_gem.png`(마름모)가
  디스크에 있지만 씬은 `ColorRect` 를 쓴다. 바꾸려면 `Sprite2D` 전환이 필요하고
  그건 `color` 주입이 조용히 죽는 종류의 변경이다
- `scripts/wave_data.gd` 의 `color` 필드는 M15에서 텍스처로 바뀐 뒤로 **렌더링에
  안 쓰인다.** 지우는 것은 별건이다
- 도로·횡단보도 같은 **더 구체적인 배경 모티프**는 넣지 않았다. 512px 타일에
  넣으면 반복이 격자처럼 읽히고 명도 대역도 벌어진다. 하려면 타일을 키워야 한다

### 밸런스

**바꾸지 않는다.** 동결이 유효하고, 아트 작업은 수치를 건드릴 이유가 없다.
다만 화면이 밝아지면 체감 난이도가 달라 보일 수 있으니 **스크린샷으로 밸런스를
판단하지 말 것** (위 🚫 참고).

**밸런스 수치를 다시 만질 때는 순서를 지킬 것** (자세한 이유는 devlog 018):

- 곡선은 손으로 적지 않는다. `tools/balance_sim.py` 의 `build_phases()` 로 생성한다
- `enemies_per_spawn` 은 1로 고정한다. 올리는 순간 스폰율이 정수배로 튄다
- **시뮬레이터로 절대 시간을 예측하지 마라.** 후보의 순서를 보는 데만 쓴다
- 한 조건당 최소 3회 실측. 3택 운으로 편차가 두 배 난다

## 리뷰에서 나왔지만 아직 안 한 것

| 항목 | 출처 | 작업량 |
|---|---|---|
| ~~레벨업 창 `grab_focus` + 숫자키 선택~~ **M21 완료** | A-designer #5 | S |
| ~~무적 시간 깜빡임~~ **M21 완료** | A-designer #7 | S |
| ~~레벨업 버튼에 현재 레벨 표시~~ **M17 완료 (별 등급)** | A-designer #8 | S |
| 화면 흔들림 최소 간격 (사망 직전 시간의 30%가 흔들림) | A-designer #9 | S |
| ~~사운드 4종~~ **M19 완료** | A-designer #10 | M |
| ~~음량 설정 UI + 저장~~ **M20 완료** | M19 잔여 | S |
| 밸런스 데이터를 외부 파일로 분리 (튜닝 루프 5단계 → 2단계) | A-engineer #7 | M |
| ~~HUD 시계와 웨이브 시계 동기화~~ **M18b 완료** | A-engineer #8 | S |
| 젬 수명·상한 | A-engineer #9 | S |
| 240마리 성능 재측정 ("풀링 불필요"는 161마리 기준 결론) | A-engineer #10 | S |
| 투사체 사거리 구현 (조준 350px인데 실제 1200px 비행) | A-engineer #11 | S |
| 반복 보스 / 페이즈 전환 알림 / 종료 조건 | A-planner | M |

## 이전 계획 (M9, 완료됨)

**목표**: 같은 게임을 "쳤다는 느낌"이 나게 만든다. 로직은 건드리지 않는다.

1. **타격 피드백** — 적이 맞으면 잠깐 흰색으로 번쩍(0.05초), 죽을 때 작은 파티클이나 확대·페이드
   - `modulate`를 잠깐 바꿨다 되돌리는 것으로 충분하다. `Tween`을 쓰면 코드가 짧다
2. **화면 흔들림** — 플레이어가 피격당했을 때 짧게. 과하면 멀미가 나므로 0.1~0.2초, 진폭 4~6px 수준
   - 카메라가 없으므로 `Main`의 `position`을 흔들거나, 이번 기회에 `Camera2D`를 넣는다. 카메라를 넣으면 **화면 경계 계산(D-007)이 전부 바뀌므로** 넣지 않는 쪽을 권장
3. **경험치 바 가시성 개선** (M8 부채) — 배경/전경 색 대비를 주기
4. **사운드** — 발사, 피격, 레벨업, 사망. 무료 CC0 효과음(예: Kenney Audio)을 받아 쓸 것. **출처와 라이선스를 devlog에 기록**
5. **아트 교체 검토** — 무료 2D 스프라이트를 쓸지, 도형을 유지할지 판단. 도형이라도 색·크기 대비만 정리하면 충분히 읽힌다
6. 테스트 — 시각 효과는 자동 검증이 어렵다. **로직 회귀가 없는지(기존 74케이스 통과)와 캡처 비교로 확인**하고, 이펙트 자체는 "일정 시간 뒤 원래 상태로 돌아오는가" 정도만 테스트

**M9 수용 기준(DoD)**: 전체 스위트 통과 + 전/후 캡처 비교 + 에셋 라이선스 기록

**주의**: M9는 게임플레이 로직을 바꾸지 않는다. 밸런스 수치 조정은 M10이다.

## 미해결 이슈 / 주의사항

| # | 내용 | 상태 |
|---|---|---|
| 1 | codex 샌드박스가 셸을 못 띄움 (`CreateProcessAsUserW failed: 5`) | **우회 완료** — codex는 파일 쓰기만, 실행·git은 Claude. 작업지시서에 셸 금지 문구 필수 |
| 2 | **헤드리스에서 뷰포트 높이가 틀리게 보고됨** (1280×720 → 1280×1280) | **우회 완료** — 화면 크기 의존 검증은 창 모드로만. 헤드리스에서는 SKIP 처리 (devlog 002) |
| 3 | 플레이어 크기 값이 여러 곳에 중복 (`body_radius` 36 / 콜라이더 36 / 스프라이트 배율) | 방치. `_clamp_to_screen()` 이 사라져 `body_radius` 는 이제 쓰이지 않는다 |
| 4 | 화면 경계를 뷰포트 기준으로 계산 중 | **M16에서 해소** — 카메라를 넣고 경계 자체를 없앴다 (D-007 번복) |
| 5 | `test.md` — 저장소 초기 테스트 파일 | 사용자가 만든 파일이라 손대지 않음 |
| 6 | codex 토큰 한도 | **한도 도달 없음** (M0~M6a 누적 약 120만 토큰). 소진 시 즉시 사용자에게 보고하고 대기 |
| 14 | **실패 원인이 3연속으로 구현이 아니라 측정 쪽이었다** (M3 캡처 도구 / M5a 람다 캡처 / M6b 테스트 격리) | 테스트가 실패하면 **구현을 고치기 전에 측정 방법부터 의심**할 것 |
| 15 | 산탄·궤도구에 레벨 개념 없음 (한 번 얻으면 끝) | M12b |
| 16 | **무적 0.5초 때문에 적 밀도가 피해량으로 번역되지 않는다** (1마리든 50마리든 초당 피해 상한이 같다) | M12b에서 재검토 (devlog 017 5절) |
| 17 | **업그레이드 25개를 다 찍으려면 레벨 26 = 약 26분**이 걸린다. 목표 플레이 시간(10~15분) 안에 빌드가 완성될 수 없다 | M12b |
| 12 | ~~codex는 이 PC에서 파일을 읽지 못한다~~ | **M21에서 정정.** codex 가 `node_repl/js` MCP 로 **파일을 읽는다** (줄 수·특정 행 내용을 물어 실제 파일과 대조해 확인). 지시서에 파일 전문을 붙일 필요가 없다. 대신 "고치기 전에 반드시 읽어라"를 명시한다. **셸 실행 불가는 그대로다** (이슈 1) |
| 13 | 안 주운 젬이 무한히 쌓임 (수명 없음) | **M16에서 악화.** 세계에 경계가 없어져 두고 온 젬으로 영영 안 돌아간다. 실측 최대 254개 |
| 18 | **난이도 곡선이 900초에서 끝난다.** 판이 그보다 길어지면 그 뒤로는 난이도 고정 | **완화** — 곡선을 가파르게 해서 900초 전에 승부가 나게 했다. 목표 시간을 더 늘리면 다시 문제가 된다 |
| 19 | 주인공(322) 대 적(40~115) — 무한한 세계에서 **도망이 항상 성립한다** | **미해결.** 밀도로 눌렀을 뿐 도망 자체는 여전히 가능하다. 적 속도 상향이 다음 후보 |
| 7 | 적들이 서로 겹쳐 한 덩어리로 뭉침 (분리 로직 없음) | M7에서 밀도 실측 후 도입 |
| 20 | **HUD 시계와 웨이브 시계 동기화** (A-engineer #8) | **M18b 해결** — 일시정지 중 시계 정지 |
| 8 | **유닛 테스트 전부 통과 ≠ 게임이 정상.** M3·M4에서 겪었다 | 마일스톤마다 창 모드 캡처 + 실제 씬 로그 확인 병행 (devlog 004·005) |
| 9 | `create_timer()`는 일시정지 중에도 진행됨 | M5 레벨업 일시정지 도입 시 캡처 타이밍 영향 확인 필요 (D-011) |
| 10 | **현재 밸런스로는 죽지 않는다** (처치 1.0/초 > 스폰 0.67/초) | M7 난이도 상승 / M10 밸런싱 (GDD 8절 실측표) |
| 11 | 사망 후 재시작 불가 (라벨만 표시) | M8 |

## 작업 규칙 (사고 후 추가)

- **스크립트를 고쳤으면 `--check-only` 문법 검사를 먼저 돌린다.** Godot 4.7은 Variant 추론(`var x := <Variant>`)을 **에러**로 취급해 스크립트 전체가 로드되지 않는다. 게임은 조용히 빈 노드로 실행된다
- **고장 주입 실험 전에 반드시 커밋한다.** 미커밋 상태에서 `git checkout -- <파일>`을 쓰면 마지막 커밋으로 돌아가 작업이 사라진다 (M4에서 실제로 발생)
- 테스트를 수정했으면 **일부러 고장을 내서 FAIL하는지 확인**한다. 통과 확인만으로는 절반이다

## 재개 절차

```powershell
# 1. 부팅 확인 (M1_BOOT_OK actions_ok=true 가 나와야 정상)
& "C:\Program Files (x86)\Godot\Godot_v4.7.1-stable_win64_console.exe" --headless --path "C:\Workspaces\game_make\test_godot_2" --quit-after 30
```

```powershell
# 2. 자동 테스트 (창 모드로 돌려야 경계 케이스까지 검증됨)
& "C:\Program Files (x86)\Godot\Godot_v4.7.1-stable_win64_console.exe" --path "C:\Workspaces\game_make\test_godot_2" "res://tests/test_player_movement.tscn"
```

`TEST_RESULT PASS`가 나오면 정상. 그 후 위 "바로 다음에 할 일"을 진행한다.
자주 쓰는 명령어 전체는 [CLAUDE.md](../CLAUDE.md) 참고.
