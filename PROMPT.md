# 작업 지시서 — Godot 서바이버류 게임 개발 (탕탕특공대 스타일)

> 이 문서는 AI(Claude Code)에게 주는 마스터 프롬프트다.
> **Claude Code가 지휘(설계·검증·문서화·git)하고, 메인 코딩은 codex CLI에 위임한다.**
> 새 세션을 시작할 때마다 이 문서를 기준으로 작업한다.
> **세션 시작 절차: `docs/STATUS.md` → 최신 개발일지 → `git log` 순으로 읽고, 진행 중인 마일스톤을 이어서 작업한다. `docs/STATUS.md`가 없으면 M0부터 시작한다.**

---

## 1. 목표

- **Godot 4.7.1**로 탕탕특공대 스타일의 **2D 탑다운 서바이버 게임**(뱀파이어 서바이버류)을 만든다.
- 테스트/학습 목적의 프로젝트다. 상용 완성도보다 **"단계마다 실행되는 결과물 + 빠짐없는 기록"**이 우선이다.
- 핵심 게임 루프: 이동 → 자동 공격 → 적 처치 → 경험치 획득 → 레벨업 시 업그레이드 선택 → 웨이브가 갈수록 강해짐 → 사망 또는 생존.

## 2. 검증된 환경 (2026-08-12 확인 완료 — 재확인 불필요)

| 항목 | 내용 |
|---|---|
| OS / 셸 | Windows 11 Pro, PowerShell 7 |
| 작업 폴더 | `C:\Workspaces\game_make\test_godot_2` (git clone 완료) |
| 원격 저장소 | https://github.com/qdwe93/godot_2 (main 브랜치, `gh` CLI로 qdwe93 계정 인증 완료 — push 가능) |
| Godot | **4.7.1 stable** — `C:\Program Files (x86)\Godot\Godot_v4.7.1-stable_win64.exe` (에디터용), `Godot_v4.7.1-stable_win64_console.exe` (CLI/헤드리스용). **PATH에 등록되어 있지 않으므로 항상 전체 경로로 호출** |
| codex CLI | 0.144.6 설치됨 — **메인 코더.** `codex exec`로 헤드리스 실행 가능. Claude보다 토큰 여유가 훨씬 많음 |
| Antigravity | IDE로 설치됨. `antigravity-ide.cmd`는 IDE를 여는 런처일 뿐 **헤드리스 에이전트 모드 없음** |
| 기타 CLI | gh, node 24, npm, python 3.14 사용 가능 |
| 연결된 MCP | 인앱 브라우저(Claude_Browser), computer-use(데스크톱 제어/스크린샷), blender, visualize 등. **Godot 전용 MCP는 미연결** |
| 보유 에셋 | `C:\Program Files (x86)\Godot`에 KayKit 등 3D 에셋 팩 존재 — 대부분 3D라 이번 2D 게임에는 부적합 |

## 3. 게임 사양 — 기본값 (사용자가 다르게 원하면 이 표만 수정)

| 항목 | 기본값 | 근거 |
|---|---|---|
| 렌더링 | 2D | 탕탕특공대가 2D 탑다운이고, 2D가 반복 개발이 훨씬 빠름 |
| 언어 | GDScript | Godot 표준. C#은 .NET 빌드 의존성이 추가되어 테스트 목적에 과함 |
| 타겟 | Windows 데스크톱 (키보드 WASD 이동) | 모바일 빌드/터치 입력은 범위 밖. 원하면 나중에 마일스톤 추가 |
| 해상도 | 1280×720, 창 모드 | 표준 16:9, 스크린샷 검증 용이 |
| 아트 | 플레이스홀더로 시작 → 무료 2D 에셋(Kenney, itch.io 등) 도입 가능 | 게임플레이 검증이 먼저. 본격적인 에셋 교체는 M9에서, 그 전이라도 공짜로 큰 이득이면 도입 가능 |
| 조준 | 자동 (가장 가까운 적) | 탕탕특공대 방식 |
| 사운드 | 후순위 (M9) | — |

## 4. 개발 방식 — 마일스톤 단위, 한 번에 하나씩

**절대 여러 기능을 한꺼번에 구현하지 않는다.** 각 마일스톤은 아래 사이클을 완결한다:

```
구현 → 실행 검증 → 커밋 + 푸시 → 문서 갱신 (STATUS.md + 개발일지)
```

| # | 마일스톤 | 완료 기준 (DoD) |
|---|---|---|
| M0 | 프로젝트 셋업 | Godot 프로젝트 생성, 폴더 규약 확정, 헤드리스 실행 확인, 문서 골격(docs/) 생성, 첫 커밋 |
| M1 | 플레이어 이동 | WASD로 플레이어가 움직이고 화면 밖으로 못 나감 |
| M2 | 적 스폰 + 추적 | 화면 밖에서 적이 주기적으로 생성되어 플레이어를 향해 이동 |
| M3 | 자동 공격 | 가장 가까운 적을 향해 일정 주기로 투사체 발사, 적 처치 가능 |
| M4 | 충돌/피해/HP | 적 접촉 시 플레이어 피해, HP 0이면 게임오버, 무적시간 |
| M5 | 경험치 + 레벨업 | 적 처치 시 경험치 젬 드랍 → 수집 → 레벨업 시 3택 업그레이드 UI (일시정지) |
| M6 | 무기/패시브 시스템 | 업그레이드가 실제로 다른 무기·스탯으로 작동 (최소 무기 3종, 패시브 3종) |
| M7 | 웨이브/난이도 | 시간에 따라 적 종류·수·강함 증가, 중간 보스 1종 |
| M8 | HUD/화면 흐름 | HP바·경험치바·타이머·킬수, 타이틀 → 플레이 → 게임오버 → 재시작 |
| M9 | 폴리시 | 타격 이펙트, 화면 흔들림, 사운드, 아트 교체(가능한 범위) |
| M10 | 밸런싱 | 수치 조정, 플레이테스트 피드백 반영 |

- 마일스톤 순서는 의존성 순서다(적이 있어야 자동 조준을 테스트할 수 있으므로 M2가 M3보다 먼저).
- 마일스톤 도중이라도 **의미 있는 변경 단위마다 커밋**한다 (토큰 소진 대비 — 아래 8절).

## 5. 검증 방법 — GUI 없이 Godot을 다루는 법

Godot은 콘솔 실행 파일로 헤드리스 제어가 가능하다. 검증은 이 순서로:

1. **문법 검사**: `Godot_..._console.exe --headless --check-only -s <스크립트>` 로 GDScript 오류 확인
2. **임포트**: 에셋 추가 후 `--headless --import` 로 임포트 캐시 생성 (이거 안 하면 CLI 실행이 리소스 못 찾고 실패함)
3. **실행 로그 검증**: `--headless` 로 씬을 몇 초간 실행해 런타임 에러/push_error 출력 확인. 자동 종료용 테스트 스크립트를 만들어 사용
4. **시각 검증**: 게임을 창 모드로 실행하고 computer-use MCP로 스크린샷을 찍어 확인. 판단이 애매하면 사용자에게 실행해 보라고 요청
5. (선택) GUT 테스트 프레임워크는 M4쯤 로직이 쌓이면 도입 여부를 판단해 DECISIONS.md에 기록

## 6. 문서 체계 — 전부 한글 마크다운, 파일명은 영문

> 파일명을 영문으로 하는 이유: Windows/git/CLI 도구 간 한글 파일명 인코딩 문제를 피하기 위해. 내용은 전부 한글.

| 파일 | 역할 | 갱신 시점 |
|---|---|---|
| `docs/STATUS.md` | **재개 가이드.** 현재 마일스톤, 마지막으로 한 일, 바로 다음 할 일, 미해결 이슈 | **모든 작업 세션 종료 시 + 긴 작업 시작 전.** 컴퓨터가 언제 꺼져도 이 파일만 읽으면 이어서 작업 가능해야 함 |
| `docs/PRD.md` | 제품 정의: 무엇을, 왜, 범위 안/밖 | M0에서 작성, 범위 변경 시 |
| `docs/GDD.md` | 게임 디자인 문서 = 기능명세: 시스템 구조, 수치 테이블(적 스탯, 무기 데미지, 경험치 곡선) | 각 마일스톤 설계 시 |
| `docs/devlog/NNN-주제.md` | 개발일지: 한 일, **판단과 근거**, **시행착오(문제 → 시도한 것 → 원인 → 해결)** | 마일스톤마다 1개 이상 |
| `docs/DECISIONS.md` | 기술 결정 기록: 선택지, 선택한 것, 이유 (예: "오브젝트 풀링 도입 여부") | 결정할 때마다 |
| `CLAUDE.md` | AI 작업 규칙 요약: 경로, 자주 쓰는 명령어, 규약 | M0에서 작성, 규약 변경 시 |

- 위 목록에 없는 형식적 문서는 만들지 않는다. 반대로, 게임에 도움이 되면(예: 밸런스 시트) 자유롭게 추가하고 STATUS.md에 링크한다.
- 시행착오는 **실패한 시도도 반드시 기록**한다. 같은 실수를 반복하지 않기 위한 문서다.

## 7. 도구·AI 활용 방침

### 역할 분담 (중요)

| 담당 | 역할 | 이유 |
|---|---|---|
| **codex CLI** | **메인 코딩**: GDScript 작성, 씬(.tscn) 구성, 버그 수정 | codex 쪽 토큰 여유가 훨씬 많다. 코드 작성량이 큰 일은 전부 codex에게 |
| **Claude Code** | 지휘: 설계, codex 작업지시서 작성, 결과 검증·코드 리뷰, git 커밋/푸시, 문서화 | 프로젝트 전체 맥락·문서·기록의 일관성 유지. Claude 토큰은 여기에만 아껴 쓴다 |

**마일스톤 진행 사이클:**

1. Claude가 설계하고, **대상 파일·구현 내용·수용 기준**을 담은 작업지시를 작성한다
2. 저장소 루트에서 `codex exec`로 codex에게 구현을 위임한다
3. Claude가 검증한다 (5절 순서: 문법 검사 → 헤드리스 실행 → 필요시 스크린샷) + 코드 리뷰
4. 문제가 있으면 구체적인 피드백과 함께 codex에 재위임한다. **자명한 1~2줄 수정만 Claude가 직접 고친다**
5. 통과하면 Claude가 커밋+푸시하고 문서를 갱신한다 (codex 위임 내용과 재작업 횟수도 개발일지에 기록)

- git 커밋·푸시는 항상 Claude가 한다 — codex에게 커밋을 시키지 않는다 (기록 일관성)
- M0에서 `codex exec --help`로 정확한 옵션(샌드박스/승인 모드, 모델, 추론 강도 지정법)을 확인하고, 검증된 **표준 호출 명령을 CLAUDE.md에 기록**해 이후 세션이 재확인 없이 쓰게 한다

### 모델·사고량 조절

- **Claude 측 최고 모델은 Opus 5.** 서브에이전트·워크플로우를 띄울 때 model을 opus 이하로 지정한다. (메인 세션의 모델은 AI가 스스로 못 바꾸므로 사용자가 모델 선택기에서 지정)
- codex의 모델·추론 강도(reasoning effort)는 작업 난이도에 맞게 지정한다: 단순 구현은 기본값, 까다로운 설계·디버깅은 높게
- Claude의 사고량: 설계·디버깅·밸런싱은 깊게, 반복 작업은 얕게. 서브에이전트·병렬화는 실익이 분명할 때만 (토큰 절약)

### 정보 검색·에셋

- **인터넷 검색과 문서 열람은 자유롭게 한다.** 특히 Godot 4.7 API는 확신이 없으면 반드시 공식 문서(docs.godotengine.org)로 확인한다 — 학습 데이터와 실제 버전의 API 차이로 인한 착오 방지
- **무료 에셋을 찾아 써도 된다** (Kenney, itch.io, OpenGameArt 등 신뢰할 수 있는 출처). 단:
  1. 다운로드 전에 출처·파일명을 사용자에게 알린다
  2. 라이선스(CC0 등)를 확인하고 상업적 제약이 있으면 쓰지 않는다
  3. 출처와 라이선스를 개발일지에 기록한다
- **Antigravity**: 헤드리스 모드가 없으므로 자동화 파이프라인에서 제외한다. 사용자가 직접 코드를 열람할 때 쓰는 IDE로만 취급
- **MCP**: 인앱 브라우저(문서 확인·에셋 탐색), computer-use(실행 화면 스크린샷 검증). Godot 전용 MCP(커뮤니티 `godot-mcp`)는 콘솔 실행 파일 직접 호출로 충분하므로 기본 미사용 — 필요해지면 DECISIONS.md에 기록 후 도입

## 8. 토큰 소진 대비 규칙 (중요)

토큰이 언제 소진될지 모른다는 전제로 작업한다:

1. **작은 커밋**: 마일스톤 완료를 기다리지 말고, 실행 가능한 상태가 될 때마다 커밋+푸시
2. **문서 먼저**: 긴 구현에 들어가기 전에 STATUS.md에 "지금부터 뭘 할 건지"를 먼저 적는다 — 도중에 끊겨도 다음 세션이 의도를 안다
3. **세션 재개 절차**: STATUS.md → 최신 devlog → `git log --oneline -10` → 마지막 커밋이 실행되는지 검증 → 이어서 작업
4. 커밋되지 않은 대량의 작업물을 쌓아두지 않는다
5. **codex 토큰 소진 시**: codex가 사용량 한도(usage/rate limit)로 멈추면 ① 즉시 STATUS.md를 갱신하고 ② **사용자에게 "codex 토큰이 소진되어 리셋이 필요하다"고 보고한 뒤 대기**한다. 사용자가 리셋해 줄 수 있다. 소진됐다고 Claude가 메인 코딩을 대신 떠맡지 않는다 (문서 정리 등 코딩 외 작업은 계속해도 됨)

## 9. 커뮤니케이션 규칙

- 사용자는 **코딩은 할 줄 알지만 Godot은 모른다.** 따라서:
  - GDScript 문법 설명 **금지**
  - 대신 다음은 **상세히 설명**: 노드/씬 구조를 왜 그렇게 잡았는지, Godot 특유의 개념(씬 트리, 시그널, 물리 레이어, `_process` vs `_physics_process` 등)이 결정에 미친 영향, 외부 도구·플랫폼 사용법, 걸어둔 제약조건
- 모든 보고와 문서는 한국어. 코드 식별자는 영문, 코드 주석은 꼭 필요한 경우만.

## 10. Git 규칙

- **main 브랜치에 직접 커밋** (1인 테스트 프로젝트이므로 브랜치 전략 불필요)
- 커밋 메시지: 한글, 1줄 요약 + 필요시 본문. 예: `M1: 플레이어 WASD 이동 구현`
- 마일스톤 완료 시 + 의미 있는 중간 단위마다 `git push origin main`
- `.godot/` 캐시 폴더는 .gitignore 처리 (M0에서 설정)

---

## 시작 명령

> **"PROMPT.md 기준으로 작업 시작해줘"** — 이 한 문장이면 된다.
> AI는 2절 재확인 없이 곧바로 STATUS.md를 읽고(없으면 M0) 다음 마일스톤 하나를 진행한다.
